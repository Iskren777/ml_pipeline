    Ключови функционалности
Конфигурационен подход (YAML): Цялата експериментална логика (кои модели да се тестват, какви хиперпараметри и какви трансформации) се контролира извън кода чрез YAML файлове в папка configs/. Това гарантира пълна репродуцируемост (reproducibility).

Разширени трансформации на данни:

За таблични данни: Автоматично тестване и сравнение на StandardScaler, MinMaxScaler, RobustScaler, PowerTransformer и липса на обработка.

За текстови данни (NLP): Интегриран потребителски трансформатор TextCleaner, който вместо агресивно да трие информация, интелигентно я токенизира (заменя съответно с _URL_, _EMAIL_, _NUM_) и тества комбинациите в решетка.

Автоматизирана моделна селекция: Интеграция на 5-Fold Крос-валидация (GridSearchCV) върху множество алгоритми едновременно (LogisticRegression с балансиране на класовете, RandomForestClassifier, KNeighborsClassifier).

Академичен и прегледен изход: Системата филтрира суровите резултати и генерира експорти с ясен контраст между представянето ПРЕДИ обработката на данните и ТОП 10 най-добри конфигурации СЛЕВ обработката, кодирани с utf-8-sig за перфектно четене на кирилица в Excel.

    Инсталация и Изисквания
Уверете се, че имате инсталиран Python (версия 3.8 или по-нова). За да инсталирате необходимите библиотеки, изпълнете следната команда в терминала:

Bash
pip install -r requirements.txt
Ако нямате файл requirements.txt, можете да инсталирате пакетите директно:

Bash
pip install pandas numpy scikit-learn matplotlib seaborn pyyaml

    Начин на стартиране (Usage)
Проектът разполага с единна интерактивна конзолна система. Стартирайте главния скрипт:

Bash
python main.py
След стартиране терминалът ще ви подкани да изберете експериментален сценарий:

Plaintext
    ДОБРЕ ДОШЛИ В АВТОМАТИЗИРАНИЯ ML PIPELINE
-------------------------------------------------
Моля, изберете тип на данните за анализ:
  [1] Таблични данни (.csv) - Влияние на мащабирането върху доходите
  [2] Текстови данни (.txt) - Влияние на почистването върху спам филтри

Въведете 1 или 2 (или 'q' за изход):
    Сценарий 1: Таблични данни (Adult Census Income Dataset)
Цел: Прогнозиране дали годишният доход надхвърля $50k на база социо-демографски данни.

Какво изследва: Доказва как алгоритми, базирани на разстояния (KNN), се провалят тотално без мащабиране на данните, докато дървовидните модели (Random Forest) остават имунни.

Артефакти: Генерира таблица results/metrics/ml_experiment_results.csv и визуализация results/figures/scaling_impact_barplot_HighRes.png.

    Сценарий 2: Неструктурирани данни (SMS Spam Collection)
Цел: Идентификация на нежелани съобщения (Спам).

Какво изследва: Показва как балансирането на класовете и запазването на числата/линковете под формата на генерализирани токени превъзхождат стандартното изтриване на символи.

Артефакти: Генерира таблица results/metrics/nlp_experiment_results.csv и SHAP-базирана графика на най-важните думи-маркери results/figures/tfidf_feature_importance.png.

    Принос към дипломната работа
Всички генерирани данни в папка results/ са форматирани по начин, който позволява:

Директно копиране на таблиците в MS Word/Excel с готови академични заглавия на колоните (на чист български език).

Директно вмъкване на графиките (експортирани с висока резолюция от 300 DPI, оптимизирани за печат на хартия).

Емпирично доказване на теоретичните постановки, заложени в Глава 1 и Глава 2 от магистърската теза.

Разработено като част от дипломен проект за ОКС "Магистър" Катедра „Информационни технологии и комуникации“, 2026 г.
"""